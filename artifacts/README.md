# Local artifacts

Place trained model files, validation reports, and exports here; generated contents are ignored by Git. The starter stores its small empirical baseline in SQLite. New model artifacts should have a version, training cutoff, feature schema, data checksum, and reproducible training command.

# Local data

This directory holds the SQLite database and local raw/processed datasets. Its contents are ignored by Git except this README. Do not commit organizer data or fabricate missing measurements. Use `examples/` for explicitly artificial, shareable development inputs. Record source lineage, timezone conversion, normalization, and checksums when implementing the real data pipeline.
